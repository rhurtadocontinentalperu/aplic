DEFINE INPUT  PARAMETER var AS CHARACTER.
DEFINE INPUT  PARAMETER len AS INTEGER.
DEFINE OUTPUT PARAMETER ret AS CHARACTER.
var = SUBSTR(var,1,len).
ret = FILL(" ",INTEGER((len - LENGTH( var )) / 2 )) + var.
RETURN.


